# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
asyncpg==0.29.0
alembic==1.13.1

# Storage
minio==7.2.0
redis==5.0.1

# Web Scraping
playwright==1.40.0
beautifulsoup4==4.12.2
requests==2.31.0
aiohttp==3.9.1

# Academic Paper Scraping
arxiv==2.1.0

# Government Document Processing
python-docx==1.1.0
openpyxl==3.1.2

# AI/ML
openai==1.6.1
langchain==0.1.0
langchain-openai==0.0.2
transformers==4.36.2
torch>=2.6.0

# PDF Processing
PyPDF2==3.0.1
pdfplumber==0.10.3

# Content Processing and Analysis
# NOTE: The following packages have compilation issues on Windows and may need special handling:
# - spacy==3.7.2 (requires blis which fails to compile)
# - pytesseract==0.3.10 (requires system dependencies)
# - easyocr==1.7.0 (requires system dependencies)
# 
# Alternative lightweight packages for text processing:
nltk==3.8.1
pillow==10.1.0
pandas==2.1.4
numpy==1.24.3
scikit-learn==1.3.2

# Data Visualization and Export
matplotlib==3.8.2
seaborn==0.13.0
plotly==5.17.0
reportlab==4.0.7
xlsxwriter==3.1.9

# Authentication and Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
email-validator==2.1.0

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6
celery==5.3.4

# Logging and Monitoring
structlog==23.2.0
prometheus-client==0.19.0

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1

# Optional: Content Processing Packages (require special installation)
# These packages may need to be installed separately or with pre-built wheels:
# 
# For spacy (if needed):
# pip install spacy
# python -m spacy download en_core_web_sm
#
# For OCR (if needed):
# pip install pytesseract
# pip install easyocr
# Note: pytesseract requires Tesseract OCR to be installed on the system
# Note: easyocr may have large model downloads on first use
